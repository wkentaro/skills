interface:
  display_name: "Setup GitHub Labels"
  short_description: "Apply and document canonical GitHub labels"
  default_prompt: "Use $setup-github-labels to preview, apply, and document the canonical labels in this repository."

policy:
  allow_implicit_invocation: false
