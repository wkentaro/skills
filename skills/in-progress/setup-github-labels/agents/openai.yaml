interface:
  display_name: "Setup GitHub Labels"
  short_description: "Apply the canonical GitHub label set"
  default_prompt: "Use $setup-github-labels to preview and apply the canonical labels to this repository."

policy:
  allow_implicit_invocation: false
